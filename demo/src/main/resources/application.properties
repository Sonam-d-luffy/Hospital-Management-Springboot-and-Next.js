# spring.application.name=demo
# server.port=${PORT:5050}
# spring.datasource.url=jdbc:postgresql://${DB_HOST}:${DB_PORT}/${DB_NAME}
# spring.datasource.username=${DB_USERNAME}
# spring.datasource.password=${DB_PASSWORD}
# spring.jpa.hibernate.ddl-auto=update
# cloudinary.cloud-name=da7fywzz0
# cloudinary.api-key=195619597651972
# cloudinary.api-secret=z912TzaTGgGWUudbY6CsQzBqMVY
# spring.servlet.multipart.max-file-size=10MB
# spring.servlet.multipart.max-request-size=10MB
# jwt.secret=1234567890123456789012345678901234567890
# jwt.expiration=86400000
# opencage.api.key=3c92f79b6af24d7783747d7268c8d31d
# spring.mail.host=smtp.gmail.com
# spring.mail.port=587
# spring.mail.username=wesportsindiaa@gmail.com
# spring.mail.password=bkbx jjtb cabk rino
# frontend.url=${FRONTEND_URL:http://localhost:3000}
# spring.mail.properties.mail.smtp.auth=true
# spring.mail.properties.mail.smtp.starttls.enable=true

spring.application.name=hospital

server.port=${PORT:5050}

spring.datasource.url=${DATABASE_URL}
spring.datasource.username=${DATABASE_USERNAME}
spring.datasource.password=${DATABASE_PASSWORD}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

cloudinary.cloud-name=${CLOUDINARY_CLOUD_NAME}
cloudinary.api-key=${CLOUDINARY_API_KEY}
cloudinary.api-secret=${CLOUDINARY_API_SECRET}

jwt.secret=${JWT_SECRET}
jwt.expiration=${JWT_EXPIRATION}

opencage.api.key=${OPENCAGE_API_KEY}

spring.mail.host=${MAIL_HOST}
spring.mail.port=${MAIL_PORT}
spring.mail.username=${MAIL_USERNAME}
spring.mail.password=${MAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=${MAIL_SMTP_AUTH}
spring.mail.properties.mail.smtp.starttls.enable=${MAIL_SMTP_STARTTLS}

frontend.url=${FRONTEND_URL:http://localhost:3000}